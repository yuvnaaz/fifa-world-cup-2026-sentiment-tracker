# ── Kafka client ──────────────────────────────────────────────────────────────
kafka-python-ng==2.2.3

# ── NLP / ML Inference ───────────────────────────────────────────────────────
# Use latest transformers + torch compatible with Python 3.14
transformers>=4.44.0
torch>=2.9.0

# ── Database Driver ───────────────────────────────────────────────────────────
# psycopg3 (psycopg) has pre-built wheels for Python 3.14; psycopg2-binary does not
psycopg[binary]>=3.1.0

# ── Dashboard / Visualization ─────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.22.0
pandas>=2.2.2

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1

# ── Additional social producers ───────────────────────────────────────────────
praw>=7.7.1                       # producer_reddit.py
websockets>=12.0                  # producer_bluesky.py (auto-installed if missing)
deep-translator>=1.11.4           # producer_bluesky.py (auto-installed if missing)
google-api-python-client>=2.130.0 # producer_youtube.py (free, official API)
requests>=2.32.0                  # producer_twitter.py (needs paid X API tier — see file header)
